version: "3"

services:
  app:
    image: oclettings
  db:
    image: nouchka/sqlite3
    volumes:
      - .:/OCLettings-app
  web:
    build: .
    volumes:
      - .:/OCLettings-app
    ports:
      - "8000:8000"
    command: "python manage.py runserver 0.0.0.0:8000"
    depends_on:
      - db
